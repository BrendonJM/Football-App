{
  "name": "spreadsheet-report-builder",
  "version": "0.1.0",
  "private": false,
  "description": "A standalone interactive report builder for Excel and CSV data.",
  "scripts": {
    "build": "node build-config.js",
    "start": "node server.js"
  }
}
